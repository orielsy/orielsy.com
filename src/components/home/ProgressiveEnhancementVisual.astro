<figure aria-label="Compact progressive enhancement model" class="home-progressive-figure m-0">
  <div class="mb-3 flex flex-wrap items-center justify-between gap-2">
    <span class="font-editorial-mono text-[8px] uppercase tracking-[0.16em] text-[var(--accent)]">
      Progressive enhancement model
    </span>
    <span class="font-editorial-mono text-[8px] uppercase tracking-[0.16em] text-[var(--text-muted)]">
      Capability can increase
    </span>
  </div>

  <div class="relative">
    <span
      aria-hidden="true"
      class="home-tier-rail absolute bottom-5 left-[4px] top-5 w-px bg-[var(--border-strong)]"
    ></span>

    <ol class="home-tier-list">
      <li class="home-tier-row">
        <span class="home-tier-node home-tier-node-baseline" aria-hidden="true"></span>
        <div class="home-tier-card home-tier-card-baseline">
          <div class="home-tier-header">
            <span class="home-tier-number">01</span>
            <span class="home-tier-title">Baseline</span>
            <span class="home-tier-badge home-tier-badge-baseline">Always on</span>
          </div>
          <p>Useful interface without inference.</p>
        </div>
      </li>

      <li class="home-tier-row">
        <span class="home-tier-node" aria-hidden="true"></span>
        <div class="home-tier-card home-tier-card-optional">
          <div class="home-tier-header">
            <span class="home-tier-number">02</span>
            <span class="home-tier-title">Local enhancement</span>
            <span class="home-tier-badge">Optional AI</span>
          </div>
          <p>Narrow intelligence when supported.</p>
        </div>
      </li>

      <li class="home-tier-row">
        <span class="home-tier-node" aria-hidden="true"></span>
        <div class="home-tier-card home-tier-card-optional">
          <div class="home-tier-header">
            <span class="home-tier-number">03</span>
            <span class="home-tier-title">Stronger inference</span>
            <span class="home-tier-badge">Optional AI</span>
          </div>
          <p>Remote capability when justified.</p>
        </div>
      </li>
    </ol>
  </div>

  <div class="mt-4 flex items-center gap-2">
    <span aria-hidden="true" class="h-px flex-1 bg-[var(--accent)]"></span>
    <span class="whitespace-nowrap font-editorial-mono text-[8px] uppercase tracking-[0.16em] text-[var(--accent)]">
      Authority stays bounded
    </span>
    <span aria-hidden="true" class="h-px flex-1 bg-[var(--accent)]"></span>
  </div>
</figure>

<style>
  .home-progressive-figure :global(p),
  .home-progressive-figure :global(ol),
  .home-progressive-figure :global(li) {
    margin: 0;
  }

  .home-progressive-figure :global(ol) {
    padding-left: 0;
  }

  .home-tier-list {
    display: grid;
    gap: 0.55rem;
  }

  .home-tier-row {
    position: relative;
    display: grid;
    grid-template-columns: 0.6rem minmax(0, 1fr);
    gap: 0.85rem;
    align-items: center;
  }

  .home-tier-node {
    position: relative;
    z-index: 1;
    width: 0.6rem;
    height: 0.6rem;
    border: 2px solid var(--accent);
    border-radius: 999px;
    background: var(--bg-card);
  }

  .home-tier-node-baseline {
    background: var(--accent);
    box-shadow: 0 0 0 2px var(--bg-card);
  }

  .home-tier-card {
    border-radius: 0.375rem;
    padding: 0.65rem 0.7rem;
  }

  .home-tier-card-baseline {
    border: 1px solid var(--accent);
    background: var(--accent-subtle);
  }

  .home-tier-card-optional {
    border: 1px dashed var(--border-strong);
    background: var(--bg-card);
  }

  .home-tier-header {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto;
    align-items: center;
    column-gap: 0.45rem;
    row-gap: 0.25rem;
    min-width: 0;
  }

  .home-tier-number {
    color: var(--accent);
    font-family: var(--font-mono-ui);
    font-size: 0.46rem;
    letter-spacing: 0.08em;
    white-space: nowrap;
  }

  .home-tier-title {
    min-width: 0;
    color: var(--text-primary);
    font-family: var(--font-mono-ui);
    font-size: 0.58rem;
    font-weight: 600;
    letter-spacing: 0.07em;
    line-height: 1.4;
    text-transform: uppercase;
  }

  .home-tier-badge {
    justify-self: end;
    white-space: nowrap;
    border: 1px solid var(--accent-border);
    border-radius: 0.2rem;
    padding: 0.08rem 0.28rem;
    color: var(--accent);
    font-family: var(--font-mono-ui);
    font-size: 0.4rem;
    letter-spacing: 0.1em;
    line-height: 1.3;
    text-transform: uppercase;
  }

  .home-tier-badge-baseline {
    border-color: var(--accent);
    background: var(--accent);
    color: var(--bg-primary);
  }

  .home-tier-card :global(p) {
    margin-top: 0.32rem;
    color: var(--text-secondary);
    font-size: 0.68rem;
    line-height: 1.45;
  }

  @media (max-width: 1180px) and (min-width: 1024px), (max-width: 460px) {
    .home-tier-header {
      grid-template-columns: auto minmax(0, 1fr);
    }

    .home-tier-badge {
      grid-column: 2;
      justify-self: start;
    }
  }
</style>
