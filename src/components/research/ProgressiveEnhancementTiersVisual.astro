<figure
  class="progressive-tiers-figure not-prose overflow-hidden rounded-xl border border-[var(--border-subtle)] bg-[var(--bg-card)]"
  aria-labelledby="progressive-tiers-title"
>
  <header class="border-b border-[var(--border-subtle)] px-5 py-5 sm:px-6">
    <div class="flex flex-wrap items-center justify-between gap-3">
      <p class="font-editorial-mono text-[9px] uppercase tracking-[0.14em] text-[var(--accent)]">
        Fig. 02 · Progressive enhancement model
      </p>
      <span class="font-editorial-mono text-[8px] uppercase tracking-[0.12em] text-[var(--text-muted)]">
        Capability can increase
      </span>
    </div>
    <h3
      id="progressive-tiers-title"
      class="font-display mt-2 max-w-3xl text-xl font-semibold tracking-[-0.03em] text-[var(--text-primary)] sm:text-2xl"
    >
      The interface remains useful before intelligence is added.
    </h3>
  </header>

  <div class="px-5 py-6 sm:px-6 sm:py-7">
    <div class="relative mx-auto max-w-3xl">
      <span
        class="tier-rail absolute bottom-6 left-[7px] top-6 w-px bg-[var(--border-strong)]"
        aria-hidden="true"
      ></span>

      <ol class="tier-list">
        <li class="tier-row tier-row-baseline">
          <span class="tier-node tier-node-baseline" aria-hidden="true"></span>
          <div class="tier-card tier-card-baseline">
            <div class="flex flex-wrap items-center gap-x-2.5 gap-y-1">
              <span class="tier-number">01</span>
              <span class="tier-title">Baseline</span>
              <span class="tier-badge tier-badge-baseline">Always on</span>
            </div>
            <p>Useful interface without inference.</p>
          </div>
        </li>

        <li class="tier-row">
          <span class="tier-node" aria-hidden="true"></span>
          <div class="tier-card tier-card-optional">
            <div class="flex flex-wrap items-center gap-x-2.5 gap-y-1">
              <span class="tier-number">02</span>
              <span class="tier-title">Local enhancement</span>
              <span class="tier-badge">Optional AI</span>
            </div>
            <p>Add narrow intelligence when the environment supports it.</p>
          </div>
        </li>

        <li class="tier-row">
          <span class="tier-node" aria-hidden="true"></span>
          <div class="tier-card tier-card-optional">
            <div class="flex flex-wrap items-center gap-x-2.5 gap-y-1">
              <span class="tier-number">03</span>
              <span class="tier-title">Stronger inference</span>
              <span class="tier-badge">Optional AI</span>
            </div>
            <p>Use remote capability only when the task justifies it.</p>
          </div>
        </li>
      </ol>
    </div>

    <div class="mx-auto mt-7 max-w-3xl">
      <div class="flex items-center gap-3">
        <span class="h-px flex-1 bg-[var(--accent)]" aria-hidden="true"></span>
        <span class="font-editorial-mono text-[8px] uppercase tracking-[0.16em] text-[var(--accent)]">
          Authority stays bounded
        </span>
        <span class="h-px flex-1 bg-[var(--accent)]" aria-hidden="true"></span>
      </div>
      <p class="mt-3 text-center text-xs leading-5 text-[var(--text-muted)]">
        Application truth and action boundaries remain stable as capability changes.
      </p>
    </div>
  </div>
</figure>

<style>
  .progressive-tiers-figure :global(p),
  .progressive-tiers-figure :global(h3),
  .progressive-tiers-figure :global(ol),
  .progressive-tiers-figure :global(li) {
    margin: 0;
  }

  .progressive-tiers-figure :global(ol) {
    padding-left: 0;
  }

  .tier-list {
    display: grid;
    gap: 0.85rem;
  }

  .tier-row {
    position: relative;
    display: grid;
    grid-template-columns: 0.95rem minmax(0, 1fr);
    gap: 0.9rem;
    align-items: center;
  }

  .tier-node {
    position: relative;
    z-index: 1;
    width: 0.9rem;
    height: 0.9rem;
    border: 2px solid var(--accent);
    border-radius: 999px;
    background: var(--bg-card);
  }

  .tier-node-baseline {
    background: var(--accent);
    box-shadow: 0 0 0 3px var(--bg-card);
  }

  .tier-card {
    border-radius: 0.5rem;
    padding: 0.95rem 1rem;
  }

  .tier-card-baseline {
    border: 1px solid var(--accent);
    background: var(--accent-subtle);
  }

  .tier-card-optional {
    border: 1px dashed var(--border-strong);
    background: var(--bg-secondary);
  }

  .tier-number {
    color: var(--accent);
    font-family: var(--font-mono-ui);
    font-size: 0.5rem;
    letter-spacing: 0.1em;
  }

  .tier-title {
    color: var(--text-primary);
    font-family: var(--font-mono-ui);
    font-size: 0.68rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    line-height: 1.5;
    text-transform: uppercase;
  }

  .tier-badge {
    margin-left: auto;
    border: 1px solid var(--accent-border);
    border-radius: 0.2rem;
    padding: 0.1rem 0.35rem;
    color: var(--accent);
    font-family: var(--font-mono-ui);
    font-size: 0.44rem;
    letter-spacing: 0.12em;
    line-height: 1.4;
    text-transform: uppercase;
  }

  .tier-badge-baseline {
    border-color: var(--accent);
    background: var(--accent);
    color: var(--bg-primary);
  }

  .tier-card :global(p) {
    margin-top: 0.45rem;
    color: var(--text-secondary);
    font-size: 0.78rem;
    line-height: 1.55;
  }

  @media (max-width: 420px) {
    .tier-badge {
      margin-left: 0;
    }
  }
</style>
