---
title: "Documentation-Driven Adaptive UX"
description: "Exploring how enterprise product knowledge can become contextual interface behavior through deterministic state, validation, authored guidance, and evidence-aware response policy."
tldr: "Product knowledge can become contextual interface behavior without giving AI authority over product truth. Phase 1 demonstrates this with a deterministic, client-side reference implementation using application state, validation, authored guidance, and explicit response policy—no LLM or remote inference required."
tldrLabel: "TL;DR — PHASE 1 REFERENCE IMPLEMENTATION"
pubDate: 2026-08-15
researchNumber: 2
updatedDate: 2026-09-13
type: "Architecture Exploration"
tags: ["ui-architecture", "context-engineering", "adaptive-ux", "enterprise-ui"]
status: "published"
published: true
featured: true
readingTime: "8 min read"
home:
  blurb: "Authored product knowledge translated into contextual interface behavior."
  visual: "adaptive-interface"
socialCard:
  variant: light
  topic: "Adaptive UX · UI Architecture"
  metadataDensity: rich
  description: "Connecting authored product knowledge to current application state."
---

import ApplicabilityVsPolicyVisual from '../../components/research/ApplicabilityVsPolicyVisual.astro';
import DocumentationDrivenAdaptiveVisual from '../../components/research/DocumentationDrivenAdaptiveVisual.astro';
import Phase1DeterministicPipelineVisual from '../../components/research/Phase1DeterministicPipelineVisual.astro';
import Phase1FindingsVisual from '../../components/research/Phase1FindingsVisual.astro';
import ResearchBreakout from '../../components/research/ResearchBreakout.astro';
import WhereAiCouldEnterLaterVisual from '../../components/research/WhereAiCouldEnterLaterVisual.astro';
import AdaptiveEnterpriseUiLab from '../../components/adaptive-ui-lab/AdaptiveEnterpriseUiLab.astro';

## The Documentation / Interface Disconnect

Enterprise products accumulate operational knowledge across documentation, release notes, runbooks, support articles, and configuration guidance. Their interfaces often expose only the raw controls.

A field may know that a value is missing. The application may know the current version and valid options. Documentation may explain why the field matters. The user is still expected to assemble that context manually.

<ResearchBreakout mode="publication">
  <DocumentationDrivenAdaptiveVisual />
</ResearchBreakout>

The opportunity is to **select and surface authored product knowledge according to current application state**, rather than make every user leave the product or receive the same static explanation.

---

## Phase 1 Experiment: One Field, Two Interface Policies

Phase 1 narrows the experiment to one product field: **Data Connection**.

The shared controls vary three pieces of context:

<ul className="paper-scenario-grid">
  <li><strong>User familiarity</strong>New or Experienced</li>
  <li><strong>Product version</strong>4.1 or 4.2</li>
  <li><strong>Compatible Data Connections</strong>One or Several</li>
</ul>

Both interfaces receive the same scenario and application truth.

### Standard UI

The Standard UI exposes the required field and ordinary validation. If the user continues without a Data Connection, it reports the missing value.

This is a normal enterprise form pattern, not a straw man: expose configuration, validate input, and rely on documentation or prior knowledge when interpretation is required.

### Adaptive UI

The Adaptive UI keeps the same field and validation rule, then adds authored product knowledge and response policy. It can surface conceptual help, version-specific guidance, and the relevant control while varying its permitted action according to the available evidence.

<ResearchBreakout mode="publication">
  <AdaptiveEnterpriseUiLab />
</ResearchBreakout>

The visible decision trace exposes application state, validation, applicable knowledge, and the resulting response policy.

---

## Deriving the Architecture

**The application remains authoritative.**

Documentation can explain a rule, but it should not redefine it. Known constraints belong in testable code or configuration; future inference can help with ambiguity without becoming the source of product truth.

The experiment separates seven concerns:

<ol className="paper-concern-grid">
  <li><strong>Domain state</strong><span>what the user has selected.</span></li>
  <li><strong>Runtime and user context</strong><span>product version, user familiarity, and available compatible options.</span></li>
  <li><strong>Validation</strong><span>deterministic application requirements.</span></li>
  <li><strong>Product knowledge</strong><span>authored explanatory material and configuration guidance.</span></li>
  <li><strong>Knowledge applicability</strong><span>whether a specific knowledge item applies to the current state.</span></li>
  <li><strong>Response policy</strong><span>how strongly the interface should intervene given the available evidence.</span></li>
  <li><strong>Rendering</strong><span>how those structured responses appear in the UI.</span></li>
</ol>

<Phase1DeterministicPipelineVisual />

Starting deterministically exposes those boundaries and creates a baseline against which later inference can be evaluated.

---

## Structured Product Knowledge

Phase 1 uses small authored knowledge fragments rather than ingested documentation: a Data Connection summary, required-field help, beginner guidance, version 4.2 guidance, and configuration-selection guidance.

Each fragment carries applicability information such as audience, version, and state conditions, plus simple provenance metadata.

The design goal is **context selection, not context accumulation**: narrow the available knowledge using deterministic facts before deciding whether any of it should affect the interface.

---

## Knowledge Applicability Is Not Response Policy

Knowing something applies does not determine what the interface should do with it.

Knowledge applicability asks:

> Does this authored information match the current product state, version, and audience?

Response policy asks:

> Given the application state, validation result, available knowledge, and certainty of the evidence, how strongly should the UI intervene?

<ApplicabilityVsPolicyVisual />

Keeping those decisions separate prevents the knowledge layer from becoming an accidental UI controller.

---

## UI Authority Should Follow Evidence

The lab uses a simple rule: **the more deterministic the evidence, the more strongly the interface may act.**

<ol className="paper-authority-scale">
  <li>no intervention</li>
  <li>expanded description</li>
  <li>contextual help</li>
  <li>current-state explanation</li>
  <li>highlight the relevant control</li>
  <li>suggest the next action</li>
  <li>preconfigure a safe value</li>
</ol>

The Data Connection experiment maps directly onto that scale. With several compatible connections, the interface may explain, highlight, and guide, but it cannot choose because user intent is unresolved. With exactly one compatible connection, it may offer that value as a user-accepted action.

Evidence changes authority; the system does not guess.

---

## What Phase 1 Shows

<Phase1FindingsVisual />

---

## Limitations of the Current Experiment

Phase 1 is a reference experiment, not a production adaptive-UX platform. It excludes:

<ul className="paper-limitations">
  <li><strong>Inference and retrieval:</strong> document ingestion, semantic search, embeddings, natural-language intent resolution, and generated guidance.</li>
  <li><strong>Production infrastructure:</strong> backend services, persistence, and authentication.</li>
  <li><strong>Learning and personalization:</strong> telemetry, behavioral learning, and inferred user profiles.</li>
  <li><strong>Runtime integrations:</strong> WebLLM, MCP, and remote AI APIs.</li>
</ul>

Product knowledge is fixture data, compatibility scenarios are fixed, and user familiarity is a simulation control.

The experiment also leaves production governance unanswered: review, stale guidance, conflicting sources, and measurement of intervention quality.

---

## Where AI Could Enter Later

The deterministic pipeline leaves an explicit seam for optional inference after context filtering and before response policy:

<ResearchBreakout mode="publication">
  <WhereAiCouldEnterLaterVisual />
</ResearchBreakout>

That seam can support intent resolution, semantic retrieval, explanation adaptation, local inference, or runtime routing while application state continues to constrain what the interface may claim or do.

---

## From Documentation to Product Capability

Long-form documentation remains necessary. But when product knowledge carries applicability, provenance, and action boundaries, it can also become an input to interface behavior instead of only a destination outside the product.

Phase 1 shows that useful adaptation does not require inference. Deterministic architecture does not eliminate AI; it defines its job: **help where ambiguity remains without becoming the source of product truth.**
